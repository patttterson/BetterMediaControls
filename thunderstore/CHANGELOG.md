# Release v1.1.1
- Moved the music folder so updates won't overwrite it. The new path is `BepInEx/config/BetterMediaControls-Music`. If you have any custom songs, move them to the new folder before updating to avoid losing them.